. ./bin/init-conda-env.sh
[[ $(which conda-init) ]] && echo "using local conda init" && source conda-init
. ./bin/shell-funcs
conda activate fastai-course

pyth=$(which python)
echo "Using python $pyth"